module ifblock

main bar;

function void foo() {
    Boolean b := false;
    debug.var("alng_scripts/testcases/statements/ifblock.alng" , "6,4,6,22" , "b", b);
    if (b) {
        global.baz();
    } else {
        global.bar();
    }
    global.print(a);
}